import 'package:flutter/material.dart';

class LeasePage extends StatelessWidget {
  const LeasePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('LeasePage'),),);
  }
}