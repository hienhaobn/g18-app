import 'package:flutter/material.dart';

class PostNewPage extends StatelessWidget {
  const PostNewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('PostNewPage'),),);
  }
}